# 电商经营数据驾驶舱 · 完整设计方案（开发交付终版）

> 本文档为最终合并版，包含全部布局、指标、图表、交互、接口、视觉与异常规范，开发可直接按模块认领并行开发。

---

## 一、项目概述与设计目标

| 项 | 说明 |
|---|---|
| 大屏名称 | XX电商经营数据驾驶舱 |
| 使用对象 | 领导层（只看结论）、运营中层（下钻分析） |
| 核心目标 | 5秒看全局 → 30秒发现问题（哪个城市/哪家店/哪个商品）→ 2分钟下钻定位 |
| 分析维度 | **时间**（今日/昨日/近7天/本月/自定义）、**城市**（全国/省/市）、**门店**（全部/单店/多选）三维度全局联动 |
| 指标体系 | 指标口径以数据中台指标字典为准（本文档第六章），命名统一，禁止前端硬编码 |

---

## 二、技术栈与架构

| 层 | 选型 |
|---|---|
| 前端框架 | Vue 3 + TypeScript + Vite |
| 图表库 | ECharts 5（按需引入：Bar/Line/Pie/Gauge/Map/EffectScatter/ScrollBoard；地图需引入 china geoJSON 及各省级 geoJSON 按需懒加载） |
| 大屏适配 | CSS `transform: scale` 等比缩放，设计稿基准 1920×1080，超宽拼接屏（3840×1080）按 1.5 倍扩展中列 |
| UI组件 | DataV 风格边框/装饰组件（自研或引用 @jiaminghi/data-view） |
| 实时通道 | WebSocket（订单流），断线自动降级为 3s 轮询，重连后自动恢复 |
| 其余数据 | RESTful + 前端定时轮询，统一走数据网关，接口带缓存（服务端 30s） |

---

## 三、整体布局与栅格规范

```
┌──────────────────────────────────────────────────────────────────┐
│ Header（h=90px）标题居中 | 左侧LOGO | 右侧时钟+日期+天气+全局筛选器      │
├──────────────────────────────────────────────────────────────────┤
│ KPI指标带（h=120px）：8张双层卡片，横向等分                              │
├──────────────┬────────────────────────────────────┬───────────────┤
│ 左列 w=24%    │        中列 w=52%                  │  右列 w=24%    │
│ L1 交易趋势   │  C1 全国城市分布地图（占中列高45%）    │ R1 城市TOP10   │
│ L2 盈利分析   │  C2 大盘分时走势（占25%）            │ R2 门店TOP10   │
│ L3 成本结构   │  C3 分时段订单分布（占15%）           │ R3 逆向健康度   │
│              │  C4 商品运营分析（占15%）             │ R4 实时订单流   │
└──────────────┴────────────────────────────────────┴───────────────┘
```

**通用规格**：
- 页面 padding=16px，模块间距 gap=12px，模块容器=DataV边框+圆角8px+深色半透明背景 `rgba(10,26,58,0.75)`；
- 每个模块标题栏高 36px：左侧 4px 竖条装饰+标题（16px 加粗），右侧“更新于 HH:mm:ss”（12px 灰色）；
- 模块内容区距标题栏 padding=8px。

---

## 四、全局交互规范（开发重点，先于模块实现）

### 4.1 全局筛选器（Header 右侧）
| 控件 | 选项 | 默认值 | 变更行为 |
|---|---|---|---|
| 时间 | 今日 / 昨日 / 近7天 / 本月 / 自定义（date-range） | 今日 | 触发全屏模块重新请求（防抖 500ms） |
| 城市 | 全国 / 省份 → 城市 级联下拉 | 全国 | 同上；选中城市后 C1 地图自动定位该省 |
| 门店 | 全部 / 多选门店（仅选了城市后可选） | 全部 | 同上 |

### 4.2 联动规则
1. 筛选器变更 → 全屏所有模块发请求，模块各自显示 loading 骨架，**不整屏白屏**；
2. C1 地图点击省份 → 地图下钻省级；点击城市气泡 → R2 自动过滤为该城市门店榜；点击空白 → 返回上级；
3. R1/R2 点击条目 → 弹出该城市/门店明细抽屉（二级指标：单均价、商品退货数量、部分/整单退款金额等下沉指标都在这里展示）；
4. 所有联动不改变 URL 状态，刷新后回到筛选默认值（领导大屏无需路由持久化）。

### 4.3 刷新频率总表
| 模块 | 刷新方式 | 频率 |
|---|---|---|
| Header 时钟 | 前端 | 1s |
| KPI 带 | 轮询 | 30s |
| L1/L2/L3、C2/C3/C4、R1/R3 | 轮询 | 60s（R1 重排动画过渡 1s） |
| C1 地图 | 轮询 | 60s |
| R2 门店榜 | 轮询 | 60s + 15s 轮播翻页（11-30名） |
| R4 订单流 | WebSocket 推送 | 实时；断线降级 3s 轮询 |

---

## 五、模块详细设计

### 5.0 Header（h=90px）
| 元素 | 规范 |
|---|---|
| 标题 | "XX电商经营数据驾驶舱"，居中，20px，字间距 4px，带底部渐变光带 |
| 时钟 | HH:mm:ss 秒级跳动（等宽数字字体）+ 日期/星期 + 天气（外部API，失败不显示，不报错） |
| 筛选器 | 见 4.1，深色主题下拉组件，与全局状态 store（Pinia）绑定 |

---

### 5.1 KPI 指标带（h=120px，8张双层卡片）

**图表类型**：数字翻牌器（主指标）+ 同比/环比箭头 + 12px sparkline 迷你趋势线（副层）。

| # | 主指标 | 副指标（12px灰字） | 告警规则 |
|---|---|---|---|
| 1 | 总营业额 | 优惠率 = 总优惠金额/总营业额 | — |
| 2 | 用户实付营业额 | 客单价 | — |
| 3 | 用户实付订单量 | 单店日均订单量 | — |
| 4 | 预计毛利 | 毛利率 | 毛利<0 主数字标红 |
| 5 | 交易用户数 | 单均价 | — |
| 6 | 商品动销率 | 动销商品数/品数（如 326/589） | <60% 标黄 |
| 7 | 门店动销 356/412 | 动销率百分比 | 动销率<80% 标黄 |
| 8 | 退款率 | 退款金额 | >5% 标红 |

**格式化规则**：金额≥1亿→"X.XX亿"，≥1万→"X.XX万"；百分比保留1位小数；环比箭头绿升红跌（毛利/毛利率为反向指标时红升绿跌，需在指标字典中标注 `direction` 字段）；空数据显示 "--"。

---

### 5.2 左列（w=24%）

#### L1 交易趋势
- **图表**：双Y轴柱线组合图
- **数据**：柱=用户实付订单量（左轴，青色 #00D4FF），折线=用户实付营业额（右轴，#FFC53D）
- **X轴**：今日/昨日→按小时(0-23)；近7天/本月→按日；上一周期同维度灰色虚线对比
- **Tooltip**：悬浮显示 营业额/订单量/客单价 三行
- **接口**：`/api/dashboard/trend?granularity=hour|day`

#### L2 盈利分析（对冲关系图，本模块是给领导看的核心洞察）
- **图表**：折线图（双线）+ 底部柱状
- **数据**：折线①毛利率（#00FFE4，左轴）；折线②优惠率（#FF9C6E，左轴同轴，形成“优惠率升↗、毛利率降↘”的可视化对冲）；底部柱=负毛利订单占比（暗红 #FF4D4F 半透明）
- **告警**：负毛利订单占比>5% 时模块标题栏右上角亮红色角标"⚠"
- **附卡**：模块底部两个小指标：单均毛利、店日均毛利

#### L3 成本&优惠结构
- **图表**：环形图（玫瑰变体），环心显示“预计线上支出”总额
- **扇区（按金额降序）**：采购成本、总优惠金额、平台配送服务费、商家自配送费用、佣金&其他平台费用、营销活动费用、推广费用、平台补贴
- **⚠口径确认项（需后端在接口文档写明）**：总优惠金额若已含平台补贴，则删除“平台补贴”扇区，**禁止重复计入**；商品成本统一命名“采购成本”
- **交互**：点击扇区弹出该费用项金额/占比/补贴率明细；右侧图例（名称+金额+占比）

---

### 5.3 中列（w=52%）

#### C1 全国城市分布地图（视觉焦点，占中列高 45%）
- **图表**：ECharts 中国地图 + EffectScatter 气泡
- **数据**：气泡大小=用户实付营业额；气泡颜色=毛利率分档（绿≥15% / 黄 0-15% / 红 <0，色阶需在图例说明）
- **侧边数据卡**（地图左侧悬浮）：覆盖城市数、TOP城市名、城市动销率（有交易的城市/覆盖城市）
- **交互**：点击省份→下钻省级地图（省级 geoJSON 懒加载）显示门店打点；点击气泡→联动 R2 过滤；空白处点击返回
- **接口**：`/api/dashboard/geo` → `[{city, lng, lat, gmv, profit_rate}]`

#### C2 大盘分时走势（占25%）
- **图表**：面积折线图，当日累计实付营业额（实心面积 #00D4FF）vs 昨日同期（灰虚线）
- **尾部标注**：差额百分比（领先绿色↑/落后红色↓）

#### C3 分时段订单分布（占15%）
- **图表**：柱状图，X轴按时段（9:00-10:00 … 17:00-18:00），Y=该时段用户实付订单量
- **峰值时段**柱体高亮 + 顶部“峰值”标签

#### C4 商品运营分析（占15%，固定模块）
- **左侧**：商品动销率环形进度（动销商品数/品数），<60% 标黄，环心显示百分比
- **右侧**：商品榜横向条形 TOP5，Tab 切换「销量榜」（商品销量，青色）/「退货风险榜」（退货率倒序，>15% 标红）
- **底部**：一行小字：商品销量合计 / 商品退货数量合计
- **接口**：`/api/dashboard/product-stats`、`/api/dashboard/product-rank?type=sales|return_rate&top=5`

---

### 5.4 右列（w=24%）

#### R1 城市 TOP10（动态排序条形图）
- **图表**：ECharts realtime-sort 横向条形图，5分钟重排，1s 动画过渡
- **Tab 指标切换**：用户实付营业额 / 预计毛利 / 用户实付订单量 / **单店日均订单量**（跨城市归一化口径，识别“店多不赚钱”的城市）
- 条形显示城市名+数值；点击条目弹出城市明细抽屉

#### R2 门店 TOP10
- **图表**：横向条形 + 轮播列表混合（全国模式15s轮播 11-30 名；单城市模式静态）
- **字段**：排名 | 门店名 | 城市 | 实付营业额 | 毛利率（负值条形标红）
- **联动**：跟随 C1 地图选中城市过滤；点击弹出门店明细抽屉（含下沉指标：单均价、动销商品数、部分/整单退款金额、商家自配送费用等）

#### R3 逆向健康度
- **图表**：4个小仪表盘 + 数字卡 + 底部堆叠条
- **主指标**：退款率（仪表盘，>5% 指针入红区）、退款金额、退款订单量、售中售后退款比
- **堆叠条**：部分退款订单量 vs 整单退款订单量
- **口径说明**：退款=订单口径；商品退货数量/商品退货率=商品口径，放在点击弹出的明细层，tooltip 注明统计维度，避免误读

#### R4 实时订单流（参考交易所大屏滚动效果）
- **图表**：无缝滚动列表（每2秒滚一条，requestAnimationFrame 恒速滚动）
- **字段**：时间 | 订单号（脱敏 ASDA5484****15）| 城市 | 门店名 | 商品摘要 | 实付金额 | 状态标签（已支付绿/已退款橙/已取消灰）
- **通道**：WebSocket `/ws/realtime-orders`，断线→状态标签变灰+提示“连接中断，轮询中”→自动降级 3s 轮询 → 重连恢复推送

---

## 六、指标字典（后端以本表为准建统一口径，前端展示名统一）

| 展示名 | 英文字段 | 口径定义 | 方向(涨好/跌好) | 展示位置 |
|---|---|---|---|---|
| 总营业额 | total_gmv | 订单原价口径含优惠 | 涨 | KPI1 |
| 总优惠金额 | discount_amount | 商家+平台全部优惠 | 观察项 | KPI1副/L3 |
| 优惠率 | discount_rate | discount_amount/total_gmv | 观察项 | KPI1副/L2 |
| 用户实付营业额 | paid_amount | 实付口径 | 涨 | KPI2 |
| 用户实付订单量 | paid_orders | 有效订单（剔除取消） | 涨 | KPI3 |
| 交易用户数 | buyer_cnt | 去重买家 | 涨 | KPI5 |
| 客单价 | arpu | paid_amount/buyer_cnt | 涨 | KPI2副 |
| 单均价 | avg_item_price | 按商品件归一均价 | 观察项 | KPI5副 |
| 单店日均订单量 | orders_per_store_day | paid_orders/动销门店数/天数 | 涨 | KPI3副/R1 |
| 预计毛利 | est_profit | 预计线上收入-预计线上支出（含平台后返口径需标注） | 涨 | KPI4 |
| 毛利率 | profit_rate | est_profit/paid_amount | 涨 | KPI4副/L2/C1 |
| 负毛利订单占比 | neg_profit_order_rate | 毛利<0订单/有效订单 | 跌 | L2 |
| 单均毛利/店日均毛利 | profit_per_order / per_store | 归一化毛利 | 涨 | L2附卡 |
| 预计线上支出 | est_expense | 成本+优惠合计 | 跌 | L3环心 |
| 采购成本 | purchase_cost | （原“商品成本”，统一改名） | 跌 | L3 |
| 商家自配送费用 | self_delivery_cost | 商家自配送口径 | 跌 | L3 |
| 品数 | sku_cnt | 在架SKU总数 | 观察项 | KPI6副 |
| 动销商品数/商品动销率 | active_sku_cnt / sku_active_rate | 有销量SKU/品数 | 涨 | KPI6/C4 |
| 商品销量 | sku_sales | 销量口径 | 涨 | C4 |
| 商品退货数量/退货率 | return_qty / return_rate | **商品口径** | 跌 | C4明细 |
| 动销门店数/覆盖门店数 | active_store_cnt / store_cnt | 有交易门店/覆盖门店 | 涨 | KPI7 |
| 退款订单量/金额/退款率 | refund_orders / refund_amount / refund_rate | **订单口径** | 跌 | KPI8/R3 |
| 售中售后退款比 | inafter_refund_ratio | 售中/售后退款 | 跌 | R3 |
| 部分退款/整单退款订单量 | partial/full_refund_orders | 订单口径 | 跌 | R3明细 |

---

## 七、接口设计

```
GET /api/dashboard/overview      → KPI带全部指标 + 环比
GET /api/dashboard/trend?granularity=hour|day&compare=1
GET /api/dashboard/geo           → [{city, lng, lat, paid_amount, profit_rate}]
GET /api/dashboard/cost-structure→ [{item, amount, rate, extra:{补贴率等}}]
GET /api/dashboard/product-stats → {sku_cnt, active_sku_cnt, sku_active_rate, sku_sales, return_qty, return_rate}
GET /api/dashboard/product-rank?type=sales|return_rate&top=5
GET /api/dashboard/rank?type=city|store&metric=paid_amount|profit|orders|orders_per_store_day&top=10
GET /api/dashboard/reverse       → 退款率/金额/订单量/售中售后比/部分整单
GET /api/dashboard/realtime-orders?page=1  （降级轮询用）
WS   /ws/realtime-orders         → 推送 {ts, order_no, city, store, item, amount, status}
```

**通用约定**：所有接口透传全局筛选参数 `date_type / start / end / city_id / store_ids`；统一响应体 `{code, data, msg}`；服务端缓存 30s；时间戳返回 `updated_at` 供模块右上角显示。

---

## 八、视觉规范

| 项 | 规范 |
|---|---|
| 底色 | #0A1A3A（深蓝科技风），模块背景 rgba(10,26,58,0.75) |
| 主色 | #00D4FF / #00FFE4；辅助 #FFC53D；告警红 #FF4D4F；告警黄 #FAAD14 |
| 数字字体 | DIN / Bahnschrift 等宽，避免跳动错位 |
| 正文字体 | PingFang SC / Microsoft YaHei |
| 动效 | 数字翻牌 600ms、图表渲染 800ms 渐入、排序过渡 1s；**禁止整屏闪烁动效** |

---

## 九、异常与边界处理（验收必查）

1. **空数据**：显示"--"，图表显示空态插画+文案“当前筛选条件下暂无数据”；
2. **加载中**：模块级骨架屏（60%透明度遮罩+呼吸动画），不整屏白屏；
3. **接口失败**：模块边框置灰+“数据加载失败”+重试按钮，其余模块正常；连续失败3次右上角全局 toast（不阻塞）；
4. **筛选极端组合**（如某城市某门店本月无数据）：全屏模块逐个显示空态，不报错；
5. **WebSocket 断网**：自动降级轮询，UI 显示降级状态，恢复后无缝切回；
6. **地图 geoJSON 加载失败**：地图区显示“地图加载失败”，侧边数据卡正常展示；
7. **数值溢出**：所有数字组件做 max-width 截断+自动缩字号（最低12px），不允许换行。

---

## 十、验收标准

- [ ] 1920×1080 及 3840×1080 拼接屏下无变形、无滚动条；
- [ ] 三个筛选器变更后 1s 内全屏出现加载态，数据 ≤3s 返回；
- [ ] 地图省→市下钻、气泡→R2联动、条目→明细抽屉，三条链路全通；
- [ ] KPI 环比方向与指标字典 `direction` 一致（反向指标不标反）；
- [ ] L3 无重复计入口径（优惠与补贴二选一，后端文档确认）；
- [ ] 断网/断WS/空数据/慢接口四类异常态演示通过；
- [ ] 大屏连续运行 24h 无内存泄漏、无图表实例堆积（定时器与 ECharts dispose 覆盖）。

---

## 十一、开发排期（建议）

| 阶段 | 内容 | 工时 |
|---|---|---|
| P1 | 框架+scale适配+Header+筛选器+Pinia全局状态 | 2天 |
| P2 | KPI带（翻牌器组件）+L1/L2/L3 | 3天 |
| P3 | C1地图（含下钻联动）+C2/C3/C4 | 3天 |
| P4 | R1/R2/R3+R4实时流 | 2天 |
| P5 | 联调、异常态、24h压测、超宽屏适配 | 1.5天 |

**并行拆分**：前端按列分3人（左列/中列/右列），翻牌器、滚动列表、明细抽屉抽为公共组件先行开发；后端按接口并行，指标字典口径评审需在开发前完成（重点是“总优惠金额与平台补贴”“退款vs退货”两处口径确认）。

以上为完整交付方案，按本文档从第三章布局 → 第五章模块 → 第六章指标字典 → 第七章接口顺序推进即可。